{
  "$schema": "foresight/verifier-eval/v1",
  "_readme": {
    "what_this_is": "A labeled corpus for measuring the Foresight reasoning verifier. Each case is a minimal known-good or known-bad implementation of one checkpoint, with a gold level a competent reviewer would assign. The harness (run-eval.mjs) runs a verifier against these and reports precision/recall — especially the false-green rate (calling a bad implementation shippable), the dangerous error.",
    "labels": "gold_level uses the 3/6/9 scale. 3 = crude/risky (the vulnerability is present). 6 = solid/shippable (the property holds). shippable = level >= 6.",
    "ids": "checkpoint ids are library ids. The corpus is ARCHETYPE-AGNOSTIC — it grades checkpoint definitions (loaded from library/checkpoints/), so one corpus covers checkpoints used by ecommerce, SaaS, portfolio, and any future archetype. Fixture directory names mirror the id.",
    "wall": "Fixtures are synthetic and contain no real project code — PATTERN-level and fully shareable. Point the verifier at a real repo separately.",
    "coverage": "Covers the high-stakes backbone checkpoints across the first three archetypes (ecommerce, SaaS, portfolio/web). Design checkpoints need rendered output and aren't fixtured as static snippets yet. Expand with more cases (and 9-level fixtures) over time."
  },
  "cases": [
    { "checkpoint": "ecommerce.checkout.atomic_stock_hold", "fixture": "fixtures/ecommerce.checkout.atomic_stock_hold/bad.ts",  "label": "bad",  "gold_level": 3, "note": "availability check and decrement are separate non-atomic steps; race window oversells" },
    { "checkpoint": "ecommerce.checkout.atomic_stock_hold", "fixture": "fixtures/ecommerce.checkout.atomic_stock_hold/good.ts", "label": "good", "gold_level": 6, "note": "atomic conditional reservation committed before the payment intent, with an expiring hold" },

    { "checkpoint": "payment.idempotency", "fixture": "fixtures/payment.idempotency/bad.ts",  "label": "bad",  "gold_level": 3, "note": "no idempotency key; webhook reprocesses on every redelivery" },
    { "checkpoint": "payment.idempotency", "fixture": "fixtures/payment.idempotency/good.ts", "label": "good", "gold_level": 6, "note": "idempotency key on creation; webhook dedupes by provider event id" },

    { "checkpoint": "payment.state_integrity", "fixture": "fixtures/payment.state_integrity/bad.ts",  "label": "bad",  "gold_level": 3, "note": "record marked paid from the client redirect, before provider confirmation" },
    { "checkpoint": "payment.state_integrity", "fixture": "fixtures/payment.state_integrity/good.ts", "label": "good", "gold_level": 6, "note": "paid set only from a verified webhook via a state machine, plus reconciliation" },

    { "checkpoint": "payment.card_data_handling", "fixture": "fixtures/payment.card_data_handling/bad.ts",  "label": "bad",  "gold_level": 3, "note": "raw PAN posted to first-party backend; secret key in client config" },
    { "checkpoint": "payment.card_data_handling", "fixture": "fixtures/payment.card_data_handling/good.ts", "label": "good", "gold_level": 6, "note": "provider token only; secret key server-side via env" },

    { "checkpoint": "payment.webhook_authenticity", "fixture": "fixtures/payment.webhook_authenticity/bad.ts",  "label": "bad",  "gold_level": 3, "note": "webhook trusts the body with no signature verification" },
    { "checkpoint": "payment.webhook_authenticity", "fixture": "fixtures/payment.webhook_authenticity/good.ts", "label": "good", "gold_level": 6, "note": "verifies provider signature against the raw body before mutating state" },

    { "checkpoint": "auth.access_control", "fixture": "fixtures/auth.access_control/bad.ts",  "label": "bad",  "gold_level": 3, "note": "fetch by id alone — IDOR" },
    { "checkpoint": "auth.access_control", "fixture": "fixtures/auth.access_control/good.ts", "label": "good", "gold_level": 6, "note": "fetch filtered by authenticated owner id" },

    { "checkpoint": "auth.session_security", "fixture": "fixtures/auth.session_security/bad.ts",  "label": "bad",  "gold_level": 3, "note": "non-expiring session, client-only logout, guessable reset token" },
    { "checkpoint": "auth.session_security", "fixture": "fixtures/auth.session_security/good.ts", "label": "good", "gold_level": 6, "note": "random short-lived sessions, server-side revocation, single-use expiring reset tokens" },

    { "checkpoint": "saas.tenancy.isolation", "fixture": "fixtures/saas.tenancy.isolation/bad.ts",  "label": "bad",  "gold_level": 3, "note": "query by id alone — crosses the tenant boundary" },
    { "checkpoint": "saas.tenancy.isolation", "fixture": "fixtures/saas.tenancy.isolation/good.ts", "label": "good", "gold_level": 6, "note": "every query scoped by tenant id from the session" },

    { "checkpoint": "saas.subscription.entitlement_integrity", "fixture": "fixtures/saas.subscription.entitlement_integrity/bad.ts",  "label": "bad",  "gold_level": 3, "note": "gate trusts a client header / a flag set once at signup" },
    { "checkpoint": "saas.subscription.entitlement_integrity", "fixture": "fixtures/saas.subscription.entitlement_integrity/good.ts", "label": "good", "gold_level": 6, "note": "entitlement from server-side subscription state updated by webhooks" },

    { "checkpoint": "saas.subscription.lifecycle", "fixture": "fixtures/saas.subscription.lifecycle/bad.ts",  "label": "bad",  "gold_level": 3, "note": "only create/cancel; failed renewal leaves status active forever" },
    { "checkpoint": "saas.subscription.lifecycle", "fixture": "fixtures/saas.subscription.lifecycle/good.ts", "label": "good", "gold_level": 6, "note": "trial-end, dunning, proration, cancel-at-period-end handled" },

    { "checkpoint": "web.performance", "fixture": "fixtures/web.performance/bad.html",  "label": "bad",  "gold_level": 3, "note": "render-blocking scripts, unsized eager images, no lazy-loading" },
    { "checkpoint": "web.performance", "fixture": "fixtures/web.performance/good.html", "label": "good", "gold_level": 6, "note": "sized/compressed/lazy images, deferred scripts, preloaded hero" },

    { "checkpoint": "web.seo_metadata", "fixture": "fixtures/web.seo_metadata/bad.html",  "label": "bad",  "gold_level": 3, "note": "generic title, no description, no Open Graph, div soup" },
    { "checkpoint": "web.seo_metadata", "fixture": "fixtures/web.seo_metadata/good.html", "label": "good", "gold_level": 6, "note": "unique title + description, Open Graph, semantic landmarks" },

    { "checkpoint": "web.forms_integrity", "fixture": "fixtures/web.forms_integrity/bad.ts",  "label": "bad",  "gold_level": 3, "note": "trusts the client, no validation or spam protection" },
    { "checkpoint": "web.forms_integrity", "fixture": "fixtures/web.forms_integrity/good.ts", "label": "good", "gold_level": 6, "note": "server-side validation + honeypot + rate limit" }
  ]
}
