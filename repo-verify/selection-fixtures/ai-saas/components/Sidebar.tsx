// Navigation sidebar — a decoy with no tracked issues.
export function Sidebar() {
  return (
    <nav className="sidebar">
      <a href="/">Home</a>
      <a href="/chat">Chat</a>
      <a href="/settings">Settings</a>
    </nav>
  );
}
